text = input("Enter text: ").upper()
a = int(input("Enter a: "))
b = int(input("Enter b: "))

cipher = ""

for ch in text:
    if ch.isalpha():
        x = ord(ch) - 65
        y = (a * x + b) % 26
        cipher += chr(y + 65)
    else:
        cipher += ch

print("Encrypted:", cipher)
