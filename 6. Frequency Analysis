text = input("Enter cipher text: ").lower()

freq = {}

for ch in text:
    if ch.isalpha():
        freq[ch] = freq.get(ch, 0) + 1

print("Letter Frequencies:")
for k, v in sorted(freq.items()):
    print(k, ":", v)
